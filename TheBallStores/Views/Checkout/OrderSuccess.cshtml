@{
    ViewData["Title"] = "Đặt hàng thành công";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var maDonHang = ViewBag.MaDonHang as int? ?? 0;
}

<div class="text-center py-5">
    <div class="mb-4">
        <i class="bi bi-check-circle-fill text-success" style="font-size: 5rem;"></i>
    </div>

    <h1 class="display-4 fw-bold text-success mb-3 text-uppercase">ĐẶT HÀNG THÀNH CÔNG!</h1>

    <p class="lead text fs-4">
        Đơn hàng của bạn <span class="text-info fw-bold">#@maDonHang</span> đã được ghi nhận.
    </p>

    <div class="card-vortex p-5 mx-auto mt-5 border border-secondary" style="max-width: 600px;">
        <h4 class="text-warning mb-4 text-uppercase">THÔNG TIN XỬ LÝ</h4>

        <p class="text mb-3">
            Bạn đã chọn phương thức <strong>Thanh toán khi nhận hàng (COD)</strong>.
        </p>

        <div class="d-flex align-items-center justify-content-center text-muted mb-2">
            <i class="bi bi-box-seam me-2 fs-5"></i>
            <span>Đơn hàng đang được chuẩn bị và sẽ giao đến bạn sớm nhất.</span>
        </div>

        <div class="d-flex align-items-center justify-content-center text-muted">
            <i class="bi bi-telephone-inbound me-2 fs-5"></i>
            <span>Nhân viên sẽ gọi điện xác nhận trước khi giao hàng.</span>
        </div>
    </div>

    <div class="mt-5 d-flex justify-content-center gap-3">
        <a asp-controller="Store" asp-action="Index" class="btn btn-neon btn-lg px-4 fw-bold">
            <i class="bi bi-cart-plus me-2"></i> TIẾP TỤC MUA SẮM
        </a>

        <!-- Đã cập nhật đường dẫn xem chi tiết -->
        <a asp-controller="DonHangs" asp-action="Details" asp-route-id="@maDonHang" class="btn btn-outline-light btn-lg px-4">
            <i class="bi bi-file-earmark-text me-2"></i> XEM CHI TIẾT ĐƠN HÀNG
        </a>
    </div>
</div>